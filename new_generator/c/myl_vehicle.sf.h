/* Automatically generated struct frame header */
/* Generated by 0.0.1 at Fri Feb  7 20:16:38 2025. */

#pragma once
#pragma pack(1)
#include "serial_frame.h"
/* Enum definitions */
typedef enum _myl_vehicle_vehicle_type {
    VEHICLE_TYPE_UNSPECIFIED = 0,
    VEHICLE_TYPE_ROVER = 1,
    VEHICLE_TYPE_BOAT = 2,
    VEHICLE_TYPE_FW = 3,
    VEHICLE_TYPE_MR = 4,
    VEHICLE_TYPE_CAR = 5
} myl_vehicle_vehicle_type;
typedef uint8_t myl_vehicle_vehicle_type;

/* Struct definitions */
typedef struct _myl_vehicle_position {
    double lat;
    double lon;
    float alt;
} myl_vehicle_position;

#define myl_vehicle_position_max_size 20;
#define myl_vehicle_position_msgid 1
MESSAGE_HELPER(myl_vehicle_position, 20, 1);



//Used for sending robot pose
typedef struct _myl_vehicle_pose {
//Used for sending roll
    float roll;
// Used for sending pitch
    float pitch;
    float yaw;
    float roll_rate;
    float pitch_rate;
    float yaw_rate;
    int16_t test;
} myl_vehicle_pose;

#define myl_vehicle_pose_max_size 26;
#define myl_vehicle_pose_msgid 2
MESSAGE_HELPER(myl_vehicle_pose, 26, 2);



typedef struct _myl_vehicle_heartbeat {
    myl_vehicle_vehicle_type type;
    int32_t id;
} myl_vehicle_heartbeat;

#define myl_vehicle_heartbeat_max_size 5;
#define myl_vehicle_heartbeat_msgid 3
MESSAGE_HELPER(myl_vehicle_heartbeat, 5, 3);



typedef struct _myl_vehicle_heartbeat2 {
    float test;
    int32_t id3;
} myl_vehicle_heartbeat2;

#define myl_vehicle_heartbeat2_max_size 8;
#define myl_vehicle_heartbeat2_msgid 4
MESSAGE_HELPER(myl_vehicle_heartbeat2, 8, 4);




uint8_t get_message_length(uint8_t msg_id){
 switch (msg_id)
 {
  case myl_vehicle_position_msgid: return myl_vehicle_position_max_size;
  case myl_vehicle_pose_msgid: return myl_vehicle_pose_max_size;
  case myl_vehicle_heartbeat_msgid: return myl_vehicle_heartbeat_max_size;
  case myl_vehicle_heartbeat2_msgid: return myl_vehicle_heartbeat2_max_size;
  default: break;
 } return 0;
}
