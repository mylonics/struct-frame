// Automatically generated frame parser
// Generated by 0.0.1 at Thu Dec  4 16:33:01 2025.

const { createFrameMsgInfo, fletcher_checksum } = require('./frame_base');

// =============================================================================
// TinyExtendedMsgIds Frame Format
// =============================================================================

const TinyExtendedMsgIdsParserState = {
    LOOKING_FOR_START: 0,
    GETTING_MSG_ID: 1,
    GETTING_LENGTH: 2,
    GETTING_PAYLOAD: 3
};

/**
 * TinyExtendedMsgIds - Frame format parser and encoder
 */
class TinyExtendedMsgIds {
    static START_BYTE = 0x72;
    static HEADER_SIZE = 4;
    static FOOTER_SIZE = 2;
    static OVERHEAD = 6;
    static LENGTH_BYTES = 1;

    constructor(get_msg_length) {
        this.get_msg_length = get_msg_length;
        this.state = TinyExtendedMsgIdsParserState.LOOKING_FOR_START;
        this.buffer = [];
        this.packet_size = 0;
        this.msg_id = 0;
        this.msg_length = 0;
    }

    reset() {
        this.state = TinyExtendedMsgIdsParserState.LOOKING_FOR_START;
        this.buffer = [];
        this.packet_size = 0;
        this.msg_id = 0;
        this.msg_length = 0;
    }

    parse_byte(byte) {
        const result = createFrameMsgInfo();

        switch (this.state) {
            case TinyExtendedMsgIdsParserState.LOOKING_FOR_START:
                if (byte === TinyExtendedMsgIds.START_BYTE) {
                    this.buffer = [byte];
                    this.state = TinyExtendedMsgIdsParserState.GETTING_MSG_ID;
                }
                break;

            case TinyExtendedMsgIdsParserState.GETTING_MSG_ID:
                this.buffer.push(byte);
                this.msg_id = byte;
                this.state = TinyExtendedMsgIdsParserState.GETTING_LENGTH;
                break;

            case TinyExtendedMsgIdsParserState.GETTING_LENGTH:
                this.buffer.push(byte);
                this.msg_length = byte;
                this.packet_size = TinyExtendedMsgIds.OVERHEAD + this.msg_length;
                this.state = TinyExtendedMsgIdsParserState.GETTING_PAYLOAD;
                break;

            case TinyExtendedMsgIdsParserState.GETTING_PAYLOAD:
                this.buffer.push(byte);

                if (this.buffer.length >= this.packet_size) {
                    const msg_length = this.packet_size - TinyExtendedMsgIds.OVERHEAD;
                    const ck = fletcher_checksum(this.buffer, 1, 1 + msg_length + 1 + 1);
                    if (ck[0] === this.buffer[this.buffer.length - 2] && ck[1] === this.buffer[this.buffer.length - 1]) {
                        result.valid = true;
                        result.msg_id = this.msg_id;
                        result.msg_len = msg_length;
                        result.msg_data = new Uint8Array(this.buffer.slice(TinyExtendedMsgIds.HEADER_SIZE, this.packet_size - TinyExtendedMsgIds.FOOTER_SIZE));
                    }
                    this.state = TinyExtendedMsgIdsParserState.LOOKING_FOR_START;
                }
                break;
        }

        return result;
    }

    static encode(msg_id, msg) {
        const output = [];
        output.push(TinyExtendedMsgIds.START_BYTE);
        output.push(msg_id);
        output.push(msg.length & 0xFF);
        for (let i = 0; i < msg.length; i++) {
            output.push(msg[i]);
        }
        const ck = fletcher_checksum(output, 1, 1 + msg.length + 1 + 1);
        output.push(ck[0]);
        output.push(ck[1]);
        return new Uint8Array(output);
    }

    static validate_packet(buffer) {
        const result = createFrameMsgInfo();

        if (buffer.length < TinyExtendedMsgIds.OVERHEAD) {
            return result;
        }

        if (buffer[0] !== TinyExtendedMsgIds.START_BYTE) {
            return result;
        }

        const msg_length = buffer.length - TinyExtendedMsgIds.OVERHEAD;

        const ck = fletcher_checksum(buffer, 1, 1 + msg_length + 1 + 1);
        if (ck[0] === buffer[buffer.length - 2] && ck[1] === buffer[buffer.length - 1]) {
            result.valid = true;
            result.msg_id = buffer[1];
            result.msg_len = msg_length;
            result.msg_data = new Uint8Array(Array.prototype.slice.call(buffer, TinyExtendedMsgIds.HEADER_SIZE, buffer.length - TinyExtendedMsgIds.FOOTER_SIZE));
        }

        return result;
    }
}

module.exports = {
    TinyExtendedMsgIds,
    TinyExtendedMsgIdsParserState,
};
