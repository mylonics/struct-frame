# Automatically generated frame parser
# Generated by 0.0.1 at Thu Dec  4 20:27:20 2025.

from enum import Enum
from typing import Callable, List, Union
from .frame_base import (
    FrameMsgInfo, fletcher_checksum,
    validate_payload_with_crc, validate_payload_minimal,
    encode_payload_with_crc, encode_payload_minimal,
)

# =============================================================================
# BasicExtendedMultiSystemStream Frame Format
# =============================================================================

class BasicExtendedMultiSystemStreamParserState(Enum):
    LOOKING_FOR_START1 = 0
    LOOKING_FOR_START2 = 1
    GETTING_MSG_ID = 2
    GETTING_LENGTH = 3
    GETTING_PAYLOAD = 4


class BasicExtendedMultiSystemStream:
    """
    BasicExtendedMultiSystemStream - Frame format parser and encoder
    
    Format: [START_BYTE1=0x90] [START_BYTE2=0x78] [MSG_ID] [LEN16] [MSG...] [CRC1] [CRC2]
    """

    START_BYTE1 = 0x90
    START_BYTE2 = 0x78
    HEADER_SIZE = 9
    FOOTER_SIZE = 2
    OVERHEAD = 11
    LENGTH_BYTES = 2

    def __init__(self, get_msg_length: Callable[[int], int] = None):
        """
        Initialize the BasicExtendedMultiSystemStream parser
        
        Args:
            get_msg_length: Callback function to get message length from msg_id
                            Required for formats without a length field
        """
        self.get_msg_length = get_msg_length
        self.reset()

    def reset(self):
        """Reset parser state"""
        self.state = BasicExtendedMultiSystemStreamParserState.LOOKING_FOR_START1
        self.buffer = []
        self.packet_size = 0
        self.msg_id = 0
        self.msg_length = 0
        self.length_lo = 0

    def parse_byte(self, byte: int) -> FrameMsgInfo:
        """
        Parse a single byte
        
        Returns:
            FrameMsgInfo with valid=True when a complete valid message is received
        """
        result = FrameMsgInfo()

        if self.state == BasicExtendedMultiSystemStreamParserState.LOOKING_FOR_START1:
            if byte == self.START_BYTE1:
                self.buffer = [byte]
                self.state = BasicExtendedMultiSystemStreamParserState.LOOKING_FOR_START2

        elif self.state == BasicExtendedMultiSystemStreamParserState.LOOKING_FOR_START2:
            if byte == self.START_BYTE2:
                self.buffer.append(byte)
                self.state = BasicExtendedMultiSystemStreamParserState.GETTING_MSG_ID
            elif byte == self.START_BYTE1:
                self.buffer = [byte]
                self.state = BasicExtendedMultiSystemStreamParserState.LOOKING_FOR_START2
            else:
                self.state = BasicExtendedMultiSystemStreamParserState.LOOKING_FOR_START1

        elif self.state == BasicExtendedMultiSystemStreamParserState.GETTING_MSG_ID:
            self.buffer.append(byte)
            self.msg_id = byte
            self.state = BasicExtendedMultiSystemStreamParserState.GETTING_LENGTH

        elif self.state == BasicExtendedMultiSystemStreamParserState.GETTING_LENGTH:
            self.buffer.append(byte)
            if len(self.buffer) == 4:
                self.length_lo = byte
            else:
                self.msg_length = self.length_lo | (byte << 8)
                self.packet_size = self.OVERHEAD + self.msg_length
                self.state = BasicExtendedMultiSystemStreamParserState.GETTING_PAYLOAD

        elif self.state == BasicExtendedMultiSystemStreamParserState.GETTING_PAYLOAD:
            self.buffer.append(byte)

            if len(self.buffer) >= self.packet_size:
                # Use shared payload validation with CRC
                result = validate_payload_with_crc(self.buffer, self.HEADER_SIZE, 2, 2)
                self.state = BasicExtendedMultiSystemStreamParserState.LOOKING_FOR_START1

        return result

    def encode(self, msg_id: int, msg: bytes) -> bytes:
        """
        Encode a message with BasicExtendedMultiSystemStream format
        
        Args:
            msg_id: Message ID
            msg: Message data bytes
        
        Returns:
            Encoded frame as bytes
        """
        output = []
        output.append(self.START_BYTE1)
        output.append(self.START_BYTE2)
        # Use shared payload encoding with CRC
        encode_payload_with_crc(output, msg_id, msg, 2, 2)
        return bytes(output)

    def encode_msg(self, msg) -> bytes:
        """Encode a message object (must have msg_id and pack() method)"""
        return self.encode(msg.msg_id, msg.pack())

    @staticmethod
    def validate_packet(buffer: Union[bytes, List[int]]) -> FrameMsgInfo:
        """
        Validate a complete BasicExtendedMultiSystemStream packet in a buffer
        
        Args:
            buffer: Buffer containing the complete packet
        
        Returns:
            FrameMsgInfo with valid=True if packet is valid
        """
        if len(buffer) < BasicExtendedMultiSystemStream.OVERHEAD:
            return FrameMsgInfo()

        if buffer[0] != BasicExtendedMultiSystemStream.START_BYTE1:
            return FrameMsgInfo()
        if buffer[1] != BasicExtendedMultiSystemStream.START_BYTE2:
            return FrameMsgInfo()

        # Use shared payload validation with CRC
        return validate_payload_with_crc(buffer, BasicExtendedMultiSystemStream.HEADER_SIZE, 2, 2)
