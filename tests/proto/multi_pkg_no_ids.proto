// Multi-package without IDs - Main package
//
// Purpose: Test behavior when importing packages without pkgid defined.
// This demonstrates the edge case where both main and imported packages
// have no package IDs.
//
package main_package;

import "no_id_pkg.proto";

message MainMessage {
  option msgid = 1;
  string name = 1 [size=32];
}
